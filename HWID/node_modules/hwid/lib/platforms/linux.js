import { execa } from '../utils.js';
const SHELL_COMMAND = 'cat /var/lib/dbus/machine-id /etc/machine-id 2> /dev/null';
const parse = raw => raw
    .toString()
    .replace(/\r+|\n+|\s+/gi, '')
    .toLowerCase();
export const linuxHWID = async () => execa(SHELL_COMMAND, parse);
