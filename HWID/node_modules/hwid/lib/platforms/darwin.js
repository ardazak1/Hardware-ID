import { execa } from '../utils.js';
const SHELL_COMMAND = 'ioreg -rd1 -c IOPlatformExpertDevice';
const parse = raw => raw
    .split('IOPlatformUUID')[1]
    .split('\n')[0]
    .replace(/=|\s+|"/gi, '')
    .toLowerCase();
export const darwinHWID = async () => execa(SHELL_COMMAND, parse);
